export class DomainPost{

    constructor(title, description, fechaVersion, version, contenido){
        this.title = title;
        this.description = description;
        this.fechaVersion = fechaVersion;
        this.version = version;
        this.contenido = contenido;
    }
}